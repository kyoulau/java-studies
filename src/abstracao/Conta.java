package abstracao;

interface Conta {
    //interface é uma classe parecida com a abstrata mas com o diferencial que podemos ter várias interfaces
    //tudo é público
    //interface é mais usada do que classes abstratas

    public void FazerPix();
    public void ConsultarSaldo();
}
